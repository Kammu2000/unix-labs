[project]
name = "unix-labs"
version = "0.1.0"
description = "unix-tools recreated in python for fun and learning"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.3",
]

[tool.ruff]
line-length = 80 
target-version = "py310"
